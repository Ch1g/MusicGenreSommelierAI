services:
  web-proxy:
    ports:
      - "8080:80"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    volumes:
      - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
